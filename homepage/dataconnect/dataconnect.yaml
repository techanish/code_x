specVersion: "v1beta"
serviceId: "homepage"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "homepage-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
